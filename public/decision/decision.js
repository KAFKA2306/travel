(()=>{'use strict';const fields=['origin','date','days','compare'];const read=()=>{const p=new URLSearchParams(location.search);fields.forEach(id=>{const el=document.getElementById(id);if(el&&p.has(id))el.value=p.get(id)})};const write=push=>{const p=new URLSearchParams;fields.forEach(id=>{const value=document.getElementById(id)?.value?.trim();if(value)p.set(id,value)});history[push?'pushState':'replaceState'](null,'',`${location.pathname}${p.size?`?${p}`:''}`);document.getElementById('state-status').textContent='旅行条件をURLへ保存しました。'};function install(){document.documentElement.dataset.kafkaSignal='kafka-signal-v1.0.0';read();fields.forEach(id=>document.getElementById(id)?.addEventListener('change',()=>write(true)));document.getElementById('copy')?.addEventListener('click',async()=>{await navigator.clipboard?.writeText(location.href);document.getElementById('state-status').textContent='共有URLをコピーしました。'});addEventListener('popstate',read)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install()})();
